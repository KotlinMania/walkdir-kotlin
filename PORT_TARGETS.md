# Port targets — `walkdir-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `walkdir` that codex-rs uses, and therefore the minimum subset that `walkdir-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-184] (rank `115`, tier `direct`, status `To Do`)

**Cargo package name**: `walkdir`
**Rust module name (in `use` statements)**: `walkdir`

## Summary

- **2** distinct symbols imported by codex-rs
- **5** total `use` statements
- **4** codex source files affected
- **4** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `walkdir::WalkDir`

- **4 `use` site(s)** across **3 codex member crate(s)**: `codex-core`, `codex-exec`, `core_test_support`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/tests/common/lib.rs:399` (`codex-core`) — `use walkdir::WalkDir`
- `core/tests/common/lib.rs:399` (`core_test_support`) — `use walkdir::WalkDir`
- `exec/tests/suite/ephemeral.rs:6` (`codex-exec`) — `use walkdir::WalkDir`
- `exec/tests/suite/resume.rs:10` (`codex-exec`) — `use walkdir::WalkDir`

### `walkdir::Error`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-git-utils`
- Aliases used: `as WalkdirError`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `git-utils/src/errors.rs:6` (`codex-git-utils`) — `use walkdir::Error as WalkdirError`
