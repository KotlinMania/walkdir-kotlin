# Rust callers — `walkdir-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `walkdir` | `walkdir` | 22 | 12 | 9 | 5 |

## `walkdir`

### Inbound symbols (5 distinct, 22 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `walkdir`.

#### `walkdir::WalkDir` — 16 site(s) (4 codex, 12 other, 10 caller crates)

- ✅ `codex-kotlin/codex-core` `tests/common/lib.rs:399` — `use walkdir::WalkDir`
- ✅ `codex-kotlin/codex-exec` `tests/suite/ephemeral.rs:6` — `use walkdir::WalkDir`
- ✅ `codex-kotlin/codex-exec` `tests/suite/resume.rs:10` — `use walkdir::WalkDir`
- ✅ `codex-kotlin/core_test_support` `lib.rs:399` — `use walkdir::WalkDir`
- `ignore-kotlin/ignore` `examples/walk.rs:2` — `use walkdir::WalkDir`
- `ignore-kotlin/ignore` `src/walk.rs:10` — `use walkdir::WalkDir`
- `insta-kotlin/insta` `src/glob.rs:7` — `use walkdir::WalkDir`
- `lalrpop-kotlin/lalrpop` `src/build/mod.rs:19` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/inotify.rs:20` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/kqueue.rs:18` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/poll.rs:79` — `use walkdir::WalkDir`
- `starlark-kotlin/starlark_bin` `bin/main.rs:30` — `use walkdir::WalkDir`
- _… and 4 more site(s)._

#### `walkdir::Error` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-git-utils` `src/errors.rs:6` — `use walkdir::Error as WalkdirError`

#### `walkdir::DirEntry` — 3 site(s) (0 codex, 3 other, 2 caller crates)

- `syn-kotlin/syn` `tests/repo/mod.rs:16` — `use walkdir::DirEntry`
- `syntect-kotlin/syntect` `examples/synstats.rs:17` — `use walkdir::DirEntry`
- `syntect-kotlin/syntect` `examples/syntest.rs:25` — `use walkdir::DirEntry`

#### `walkdir` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `zstd-kotlin/zstd` `src/dict.rs:250` — `use walkdir`

#### `walkdir::DirEntryExt` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `ignore-kotlin/ignore` `src/walk.rs:218` — `use walkdir::DirEntryExt`
