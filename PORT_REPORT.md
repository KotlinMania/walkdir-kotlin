# Port report — `walkdir-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `walkdir`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 22 `use` sites from 9 other repos
- **Outbound**: 2 `use` sites to 1 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `walkdir` | `walkdir` | 22 | 1 | COD-184 | 115 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (22 edges, 12 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `syntect-kotlin` | `syntect` | 5 |  |
| `ignore-kotlin` | `ignore` | 3 |  |
| `notify-kotlin` | `notify` | 3 |  |
| `codex-kotlin` | `codex-exec` | 2 | ✅ |
| `syn-kotlin` | `syn` | 2 |  |
| `codex-kotlin` | `codex-core` | 1 | ✅ |
| `codex-kotlin` | `codex-git-utils` | 1 | ✅ |
| `codex-kotlin` | `core_test_support` | 1 | ✅ |
| `insta-kotlin` | `insta` | 1 |  |
| `lalrpop-kotlin` | `lalrpop` | 1 |  |
| `starlark-kotlin` | `starlark_bin` | 1 |  |
| `zstd-kotlin` | `zstd` | 1 |  |

### Symbol-level demand (5 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `walkdir::WalkDir` | 16 | 4 | 12 | 10 |
| `walkdir::Error` | 1 | 1 | 0 | 1 |
| `walkdir::DirEntry` | 3 | 0 | 3 | 2 |
| `walkdir` | 1 | 0 | 1 | 1 |
| `walkdir::DirEntryExt` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 5 symbols)

##### `walkdir::WalkDir` (16 sites, 4 codex, 12 other, 10 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `tests/common/lib.rs:399` — `use walkdir::WalkDir`
- `codex-kotlin/codex-exec` `tests/suite/ephemeral.rs:6` — `use walkdir::WalkDir`
- `codex-kotlin/codex-exec` `tests/suite/resume.rs:10` — `use walkdir::WalkDir`
- `codex-kotlin/core_test_support` `lib.rs:399` — `use walkdir::WalkDir`

Other workspace call sites:

- `ignore-kotlin/ignore` `examples/walk.rs:2` — `use walkdir::WalkDir`
- `ignore-kotlin/ignore` `src/walk.rs:10` — `use walkdir::WalkDir`
- `insta-kotlin/insta` `src/glob.rs:7` — `use walkdir::WalkDir`
- `lalrpop-kotlin/lalrpop` `src/build/mod.rs:19` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/inotify.rs:20` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/kqueue.rs:18` — `use walkdir::WalkDir`
- `notify-kotlin/notify` `src/poll.rs:79` — `use walkdir::WalkDir`
- `starlark-kotlin/starlark_bin` `bin/main.rs:30` — `use walkdir::WalkDir`

_… 4 more call sites._

##### `walkdir::Error` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-git-utils` `src/errors.rs:6` — `use walkdir::Error as WalkdirError`

##### `walkdir::DirEntry` (3 sites, 0 codex, 3 other, 2 caller crates)

Other workspace call sites:

- `syn-kotlin/syn` `tests/repo/mod.rs:16` — `use walkdir::DirEntry`
- `syntect-kotlin/syntect` `examples/synstats.rs:17` — `use walkdir::DirEntry`
- `syntect-kotlin/syntect` `examples/syntest.rs:25` — `use walkdir::DirEntry`

##### `walkdir` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `zstd-kotlin/zstd` `src/dict.rs:250` — `use walkdir`

##### `walkdir::DirEntryExt` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `ignore-kotlin/ignore` `src/walk.rs:218` — `use walkdir::DirEntryExt`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `winapi-util-kotlin` | 2 | 2 | `winapi-util` | COD-190 | 114 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `same-file` | 2 | 1 | `walkdir` |
